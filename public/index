<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8"/>
  <meta name="viewport" content="width=device-width, initial-scale=1"/>
  <title>YourBrand Calls</title>
  <style>
    body{font-family:system-ui,-apple-system,Segoe UI,Roboto,Ubuntu,Helvetica,Arial,sans-serif;
         background:#0f1115;color:#e6e6e6;margin:0;padding:24px}
    .card{max-width:720px;margin:0 auto;background:#141824;border:1px solid #23283b;border-radius:12px;padding:24px}
    h1{margin:0 0 8px}
    p{margin:0 0 24px;color:#a8b0c0}
    label{display:block;margin:14px 0 6px;font-weight:600}
    input{width:100%;padding:10px 12px;border-radius:8px;border:1px solid #2b3250;background:#0f1320;color:#e6e6e6}
    .grid{display:grid;grid-template-columns:1fr 1fr;gap:12px}
    button{margin-top:18px;background:#6c8cff;border:0;border-radius:10px;padding:12px 16px;
           font-weight:700;color:#0f1115;cursor:pointer}
    button:disabled{opacity:.6;cursor:not-allowed}
    .links{margin-top:18px;background:#0f1320;border:1px solid #2b3250;border-radius:10px;padding:14px}
    .links a{display:block;color:#8bd0ff;margin:6px 0;word-break:break-all;text-decoration:none}
    .ok{color:#64e39b}
    .err{color:#ff8c8c}
  </style>
</head>
<body>
  <div class="card">
    <h1>YourBrand Calls</h1>
    <p>Set duration & names, then press <b>Start</b>. Rooms are created <span class="ok">Public</span> by default.</p>

    <div class="grid">
      <div>
        <label>Duration (minutes)</label>
        <input id="duration" type="number" min="1" value="30"/>
      </div>
      <div>
        <label>Manager name</label>
        <input id="manager" type="text" placeholder="Manager" value="Manager"/>
      </div>
    </div>

    <div class="grid">
      <div>
        <label>Client name</label>
        <input id="client" type="text" placeholder="Client" value="Nick"/>
      </div>
      <div>
        <label>Model name</label>
        <input id="model" type="text" placeholder="Model" value="Anna"/>
      </div>
    </div>

    <button id="start">Start</button>
    <div id="status"></div>

    <div id="out" class="links" style="display:none">
      <div><b>Links</b></div>
      <a id="link-client" target="_blank" rel="noopener"></a>
      <a id="link-model"  target="_blank" rel="noopener"></a>
      <a id="link-manager" target="_blank" rel="noopener"></a>
    </div>
  </div>

  <script src="./main.js"></script>
</body>
</html>
